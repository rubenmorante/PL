
Tabla de Símbolos
      Ámbito

Ámbito
      HashMap<K, V> <-gfgfs, -sadad, -dasd, -adsad>
            K : String id
            V : Object

// Hacer clase padre de los objectos de semánticos.

Commands
      String id
      Enum Type


Función
      HashMap <K, V> -> Ámbito.



Comportamiento
--------------

boolean InsertaId(Enum Tipo, String id, Object value);

Object BuscaId(String id);

boolean InsertaTipo(String id, Enum Type)

Enum Type DameTipo(String id)

bool TiposComp(Enum Type tipo1, Enum Type tipo2)

bool esMatriz( Enum Tipo );

int DaTamaño( Enum Tipo );

Enum Type DaTipoDest( Enum Type );



Tipos
-------
